{
  "name": "@personal-ai/desktop-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Siri-style Windows desktop assistant UI (glowing orb)",
  "main": "main.cjs",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "build:renderer": "esbuild renderer/app.js --bundle --format=iife --outfile=renderer/app.bundle.js --platform=browser --minify"
  },
  "devDependencies": {
    "electron": "^43.4.0"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
